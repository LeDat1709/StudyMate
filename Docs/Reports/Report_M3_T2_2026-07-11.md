# Report — M3-T2 Form tạo Job (Student) — 2026-07-11

> **Task:** M3-T2 — Create Job  
> **FRS:** UC-M3-01  
> **Prompt:** `Docs/Prompts/M3_T2_CreateJob.xml`  
> **Branch:** `feat/m3-job-posting`

---

## Đã làm gì

- `ViewModels/JobPosting/JobPostingFormViewModel.cs` — validation Title, Description min 50, SubjectId, TeachingMode, budgets, deadline…
- `JobPostingController` Create GET/POST `[Authorize(Roles=Student)]`
  - Validate Offline/Both → Address
  - Max 5 job Status=Open / student
  - Lưu Status=Open, redirect Details
- Views `Create.cshtml` + `_Form.cshtml` (Bootstrap 5)

### Logic Create (tóm tắt)

1. Chỉ Student  
2. ModelState + business rules  
3. Count Open &lt; 5  
4. Insert JobPosting  
5. PRG → Details  

---

## Kết quả

- Build: ✅ Pass
- Test Plan: ✅ Ready (smoke với account Student)

### Acceptance Criteria

| Tiêu chí | Status |
|---|---|
| Form đủ field FRS | ✅ |
| Description &lt; 50 → lỗi | ✅ |
| Offline không Address → lỗi | ✅ |
| Max 5 Open | ✅ |
| Status Open sau create | ✅ |
| Guest/Tutor bị chặn | ✅ |

### Test Plan

1. Login Student → `/JobPosting/Create`  
2. Submit thiếu → validation  
3. Submit OK → DB Open  
4. Tạo 6 job Open → job 6 fail  

---

## Rủi ro

- Redirect Details khi T4 chưa merge trên branch độc lập — đã gộp controller đầy đủ trên stack.
- Subjects rỗng nếu chưa seed (cần app start SeedSubjects).

## Hiểu được gì

- Business max Open đếm theo Status string `"Open"`.
- Address clear khi TeachingMode=Online.

## Bước tiếp theo

- **M3-T3** Public list + paging
