# Report — M4-T2 IMatchingService stub scoring — 2026-07-11

> **Task:** M4-T2  
> **FRS:** UC-M4-01 fallback  
> **Prompt:** `Docs/Prompts/M4_T2.xml`  
> **Branch:** `feat/m4-ai-matching`

---

## Đã làm gì

- `IMatchingService` + `MatchingService`:
  - `MatchJobToTutorsAsync` — score môn + keyword + budget, top 10, stub-v1
  - `RecommendJobsForTutorAsync` — gợi ý job Open theo subject
- Chỉ tutor `IsVerified && IsAvailable`
- Xóa matching cũ cùng JobPostingId trước insert
- DI Scoped trong `Program.cs`

---

## Kết quả

- Build: ✅ Pass

### Acceptance Criteria

| Tiêu chí | Status |
|---|---|
| DI inject OK | ✅ |
| Top ≤10, Rank theo score | ✅ |
| 0 tutor không crash | ✅ |
| build pass | ✅ |

### Test Plan

1. `dotnet build`  
2. Create job (T3) → rows MatchingResults  

---

## Rủi ro

- Stub ≠ embedding cosine thật; production cần Python FastAPI + timeout 10s + fallback (đã là fallback).

## Hiểu được gì

- FRS cho phép fallback khi AI down — stub chính là fallback mặc định.

## Bước tiếp theo

- **M4-T3** Gắn Create job + Details UI
