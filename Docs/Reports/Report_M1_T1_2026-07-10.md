# Report — M1-T1 Email Service — 2026-07-10

## Đã làm gì
- Cài package `MailKit` 4.17.0
- Tạo `Services/Interfaces/IEmailService.cs`
- Tạo `Services/Implementations/EmailService.cs` — dùng MailKit STARTTLS, đọc config từ `appsettings.json`
- Cập nhật `Program.cs` — thêm section 4 Application Services, đăng ký `AddScoped<IEmailService, EmailService>()`
- Tạo `.gitignore` — exclude `appsettings.Development.json` và build artifacts
- Tạo `Docs/Prompts/M1_T1_EmailService.xml`

## Kết quả
- Build: ✅ Pass (`dotnet build` — 0 error, 0 warning)
- Test Plan: ✅ Pass
  - Email nhận được tại `letandat1709@gmail.com`
  - Subject: `[StudyMate] Test EmailService M1-T1`
  - Sender hiển thị đúng: `StudyMate Dev <letandat1709@gmail.com>`
- Acceptance Criteria: ✅ Tất cả đạt
- Code test tạm thời trong `HomeController` đã được xóa sau khi verify

## Rủi ro phát hiện
- `EmailService` không throw exception khi gửi fail — chỉ log error. Đây là thiết kế cố ý để tránh crash app, nhưng các task sau (T3 Register, T6 ForgotPassword) cần kiểm tra log để biết email có thực sự gửi đi không.
- Gmail App Password đang lưu trong `appsettings.Development.json` — đã exclude khỏi git bằng `.gitignore`. Production cần dùng environment variable trên server.

## Hiểu được gì
- MailKit dùng `SecureSocketOptions.StartTls` cho port 587 — khác với SSL port 465
- Pattern inject `IConfiguration` trực tiếp vào service thay vì dùng `IOptions<T>` — đơn giản hơn cho config nhỏ, đủ dùng cho scope hiện tại
- `AddScoped` phù hợp hơn `AddSingleton` cho EmailService vì `SmtpClient` của MailKit không thread-safe

## Bước tiếp theo
- Task tiếp theo: **M1-T2 OTP Service**
- Cần chuẩn bị: `OtpCode.cs` và `ApplicationDbContext` đã sẵn sàng — T2 có thể bắt đầu ngay
- Tạo `Docs/Prompts/M1_T2_OtpService.xml` trước khi giao agent
