# Report — M3-T1 JobPosting Model + Migration — 2026-07-11

> **Task:** M3-T1 — JobPosting Model + Migration  
> **FRS:** `Docs/FRS/FRS_M3_JobPosting.md` (bảng JobPostings)  
> **Prompt:** `Docs/Prompts/M3_T1_JobPostingModel.xml`  
> **Branch:** `feat/m3-job-posting`  
> **Phụ thuộc:** M1 Identity, M2 Subjects

---

## Đã làm gì

- Tạo `Models/JobPosting.cs` — map schema: StudentId, Title, Description, SubjectId, DesiredLevel, TeachingMode, Address, BudgetMin/Max, SessionsPerWeek, SessionDuration, Deadline, Status (default Open), timestamps + navigation Student/Subject
- Cập nhật `ApplicationDbContext`:
  - `DbSet<JobPosting>`
  - Fluent: precision budget, max length, FK Restrict Student/Subject, index StudentId + (Status, Deadline)
- Migration `AddJobPosting`
- Tạo prompt `M3_T1_JobPostingModel.xml`

---

## Kết quả

- Build: ✅ Pass (`dotnet build` — 0 error)
- Migration: ✅ Generated
- Acceptance Criteria: ✅ theo BreakTask M3-T1
- Out of scope giữ đúng: không Controller/View, không Application/Matching

### Acceptance Criteria

| Tiêu chí | Status |
|---|---|
| Entity đủ field theo schema | ✅ |
| Migration tạo JobPostings + FK | ✅ |
| Index Status/Deadline | ✅ |
| `dotnet build` pass | ✅ |

### Test Plan (checklist)

1. `dotnet build` → pass  
2. Xem migration `Up()` có `CreateTable("JobPostings")`  
3. (Dev) `dotnet ef database update` → bảng tồn tại  

---

## File thay đổi

| File | Hành động |
|---|---|
| `StudyMate/Models/JobPosting.cs` | Tạo mới |
| `StudyMate/Data/ApplicationDbContext.cs` | Sửa |
| `StudyMate/Migrations/*AddJobPosting*` | Tạo mới |
| `Docs/Prompts/M3_T1_JobPostingModel.xml` | Tạo mới |

---

## Rủi ro phát hiện

- Status dùng string — typo có thể vào DB; có thể siết bằng check constraint sau.
- DeleteBehavior Restrict trên Student: xóa user có job sẽ fail — đúng bảo toàn lịch sử.

## Hiểu được gì

- Job thuộc Student (UserId), không thuộc TutorProfile.
- SubjectId bắt buộc FK — Create form phải load Subjects seed M2.

## Bước tiếp theo

- **M3-T2** Form Create Job (Student) + validation + max 5 Open
