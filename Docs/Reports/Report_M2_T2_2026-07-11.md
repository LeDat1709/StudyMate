# Report — M2-T2 Related Models + Certificate FK — 2026-07-11

> **Task:** M2-T2 — Availability + DemoLesson + migrate Certificate → TutorProfileId  
> **Prompt:** `Docs/Prompts/M2_T2_RelatedModelsAndCertificateFk.xml`  
> **Branch:** `feat/m2-tutor-profile`

## Đã làm gì

- Tạo `Models/TutorAvailability.cs` — DayOfWeek 0–6 (Sun–Sat), StartTime, EndTime
- Tạo `Models/DemoLesson.cs` — Title, Description, VideoUrl
- Sửa `TutorCertificate`: bỏ `UserId`, thêm `TutorProfileId` + navigation
- Navigation collections trên `TutorProfile` (Certificates, Availabilities, DemoLessons)
- DbContext Fluent API + DbSets
- Migration data-safe:
  1. Add nullable `TutorProfileId`
  2. `UPDATE` map qua `TutorProfiles.UserId`
  3. `DELETE` orphan certs (chưa có profile)
  4. Drop `UserId`, set `TutorProfileId` NOT NULL + FK
- `AccountController` Certificates: resolve profile; block upload nếu chưa có hồ sơ

## Kết quả

- Build: ✅ Pass
- AC: ✅ đạt theo BreakTask M2-T2

## Rủi ro

- Cert orphan (user chưa có TutorProfile) **bị xóa** khi migrate — chấp nhận được trên dev; production cần backup.
- Upload cert qua Account **cần profile** — UI Create profile ở M2-T4.

## Next

- **M2-T3** FileStorageService
