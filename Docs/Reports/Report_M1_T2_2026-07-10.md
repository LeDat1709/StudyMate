# Report — M1-T2 OTP Service — 2026-07-10

## Đã làm gì
- Thêm field `FailedAttempts int = 0` vào `OtpCode.cs`
- Tạo migration `AddOtpFailedAttempts`, apply lên DB `StudyMate`
- Tạo `Services/Interfaces/OtpValidationResult.cs` — enum 5 giá trị
- Tạo `Services/Interfaces/IOtpService.cs` — 3 method signatures
- Tạo `Services/Implementations/OtpService.cs` — đọc config từ `appsettings.json`
- Cập nhật `Program.cs` — thêm `AddScoped<IOtpService, OtpService>()`
- Cập nhật `Database/StudyMate_Schema.sql` và `StudyMate_PostgreSQL.sql` — thêm column `FailedAttempts`
- Tạo `Database/SeedUsers.sql` — 3 user dev (Admin/Tutor/Student), password `Test@1234`
- Fix `appsettings.Development.json` — bị mất credential do checkout nhánh khác

## Kết quả Test
- Build: ✅ Pass
- Test Plan: ✅ Pass — tất cả 4 case đúng kỳ vọng:

| Case | Input | Kết quả | Mong đợi |
|---|---|---|---|
| T1 | GenerateAndSaveOtpAsync | OTP 6 số được tạo | ✅ |
| T2 | ValidateOtpAsync(đúng mã) | Valid | ✅ |
| T3 | ValidateOtpAsync(sai 1,2 lần) | Invalid | ✅ |
| T3 | ValidateOtpAsync(sai lần 3) | TooManyAttempts | ✅ |
| T4 | ValidateOtpAsync(mã cũ đã dùng) | TooManyAttempts | ✅ |

## Rủi ro phát hiện
- `appsettings.Development.json` dễ bị mất khi checkout nhánh hoặc git operation — cần nhớ restore thủ công sau mỗi lần checkout. Cân nhắc dùng `dotnet user-secrets` cho các project sau.
- Password hash trong `SeedUsers.sql` là hash cứng — nếu Identity key rotation xảy ra thì hash không còn valid. Chỉ dùng cho dev.
- `OtpService` không inject `IEmailService` — đúng thiết kế, nhưng các task sau (T3, T6) cần inject cả 2 service vào Controller.

## Hiểu được gì
- `FailedAttempts` lưu trực tiếp trên record OTP sạch hơn so với đếm query — dễ audit, dễ reset
- `InvalidateAllOtpAsync` được gọi trước `GenerateAndSaveOtpAsync` — đảm bảo mỗi user chỉ có 1 OTP active per purpose tại 1 thời điểm
- ASP.NET Identity hash password không thể seed bằng plain text SQL — phải dùng hash được tính trước

## Bước tiếp theo
- Task tiếp theo: **M1-T3 Register Page + ViewModels + Validation**
- Cần chuẩn bị: `IEmailService` + `IOtpService` đã sẵn — T3 có thể bắt đầu ngay
- Tạo `Docs/Prompts/M1_T3_Register.xml` trước khi giao agent
