# Report — M1-T3 Register Page — 2026-07-10

## Đã làm gì
- Tạo `ViewModels/Account/RegisterViewModel.cs` — validation đầy đủ: FullName, Email, Password (regex), ConfirmPassword, Role
- Tạo `Controllers/AccountController.cs` — Register GET/POST, VerifyEmail GET placeholder
- Tạo `Views/Account/Register.cshtml` — Bootstrap 5, radio button Student/Tutor, jQuery Unobtrusive validation
- Tạo `Views/Account/VerifyEmail.cshtml` — placeholder, hiển thị email từ TempData

## Kết quả
- Build: ✅ Pass
- Test Plan: ✅ Pass
  - Form hiển thị đúng layout, đủ field
  - Validation inline hoạt động, không reload trang
  - Email đã tồn tại → lỗi "Email này đã được sử dụng"
  - Submit hợp lệ → user tạo trong DB, EmailConfirmed=0, OTP gửi đến email, redirect VerifyEmail

## Rủi ro phát hiện
- TempData["RegisteredEmail"] chỉ tồn tại 1 request — nếu user refresh trang VerifyEmail sẽ bị redirect Register. Đây là thiết kế cố ý (bảo mật), nhưng UX hơi khó chịu. Sẽ xử lý ở T4.
- Email template OTP viết inline trong Controller — nếu sau này cần thay đổi template phải sửa code. Có thể refactor sang partial view sau khi hoàn thành M1.

## Hiểu được gì
- `UserManager.CreateAsync` tự hash password — không cần xử lý thủ công
- `AddToRoleAsync` phải gọi sau `CreateAsync` thành công — role được gán qua `AspNetUserRoles`
- TempData phù hợp để truyền dữ liệu nhỏ giữa redirect, không dùng Session

## Bước tiếp theo
- Task tiếp theo: **M1-T4 VerifyEmail + OTP validation**
- Cần chuẩn bị: TempData["RegisteredEmail"] đã có, IOtpService sẵn sàng
