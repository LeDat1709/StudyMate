# Report — M1-T7 Profile + Upload Avatar — 2026-07-10

## Đã làm gì
- Cập nhật `StudyMate/Controllers/AccountController.cs`:
  + Thêm `Profile` GET để hiển thị thông tin người dùng hiện tại
  + Thêm `Profile` POST để lưu `FullName` và `PhoneNumber`
  + Thêm `UploadAvatar` POST để upload avatar qua AJAX
- Cập nhật `StudyMate/ViewModels/Account/ProfileViewModel.cs` để hỗ trợ `Email`, `FullName`, `PhoneNumber`, `AvatarUrl`
- Cập nhật `StudyMate/Views/Account/Profile.cshtml`:
  + Hiển thị email read-only
  + Form sửa profile
  + Upload avatar bằng input file và AJAX
  + Hiển thị avatar mới ngay sau khi upload thành công
- Tạo placeholder thư mục `StudyMate/wwwroot/uploads/avatars/.gitkeep` để lưu avatar

## Kết quả
- Build: ✅ Pass
- Feature ready theo yêu cầu M1-T7
- Avatar upload validation:
  - Chỉ chấp nhận định dạng JPG/JPEG, PNG, WEBP
  - Giới hạn kích thước file tối đa 2MB
- Avatar lưu với tên file `{userId}_{timestamp}.{ext}` và cập nhật `ApplicationUser.AvatarUrl`
- `UploadAvatar` trả JSON với `url` mới để client cập nhật ảnh ngay

## Rủi ro phát hiện
- Nếu người dùng upload nhiều avatar, file cũ không được xóa tự động; có thể gây tích trữ file theo thời gian.
- `Profile` POST không sửa email; điều này đúng scope nhưng nếu cần đổi email sẽ cần task riêng.
- Nếu app không có thư mục `wwwroot/uploads/avatars`, controller tạo mới khi upload; cần kiểm tra quyền ghi file trên môi trường deploy.

## Hiểu được gì
- `Email` trên Profile phải là `disabled` để không thay đổi từ form
- Upload avatar nên xử lý bằng AJAX để người dùng thấy ảnh mới ngay mà không reload toàn bộ page
- Dùng `TempData` để báo thành công khi redirect sau POST Profile

## Bước tiếp theo
- Task tiếp theo: **M1-T8 Change Password** (hoặc các task Profile tiếp theo nếu cần mở rộng)
- Nên kiểm tra trực tiếp các trường hợp:
  1. `/Account/Profile` khi đã login hiển thị đúng
  2. Sửa `FullName`/`PhoneNumber` và reload trang
  3. Upload avatar PNG < 2MB → ảnh thay đổi ngay
  4. Upload file > 2MB → lỗi rõ ràng
  5. Upload file không đúng định dạng → lỗi rõ ràng
